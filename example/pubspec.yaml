name: stratosfy_scanner_example
description: Demonstrates how to use the stratosfy_scanner plugin.
publish_to: "none"

environment:
  sdk: ">=2.12.0 <3.0.0"

dependencies:
  flutter:
    sdk: flutter
  stratosfy_scanner:
    path: ../
  intl:
  platform: 3.1.4
  shared_preferences: 2.2.2

dev_dependencies:
  flutter_test:
    sdk: flutter

flutter:
  uses-material-design: true
  